zone "altay.insecure" {
    type forward;
    forward only;
    forwarders { 192.168.56.100; };  // Zararlı alan adını alan saldırganın sunucusuna yönlendirme
};

zone "altay.sec" {
    type forward;
    forward only;
    forwarders { 192.168.56.10; }; // Şirketin domainleri firewall'a soruluyor
};